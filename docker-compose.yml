version: "3"
services:
  web:
    container_name: ionic
    build: .
    volumes:
      - .:/app
    ports:
      - 8100:8100
    entrypoint:
      - sh
    command:
      - -c
      - |
        [ -f ~/.exec-once ] || {
          yarn install && touch .exec-once
        }
        yarn start